{
  "name": "desafio-do-gatinho-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app-expo",
    "packages/electron"
  ],
  "scripts": {
    "dev":           "npm run dev --workspace=app-expo",
    "dev:web":       "npm run dev:web --workspace=app-expo",
    "build:web":     "npm run build:web --workspace=app-expo",
    "build:win":     "npm run build:win --workspace=packages/electron",
    "build:apk":     "npm run build:apk --workspace=app-expo",
    "build:aab":     "npm run build:aab --workspace=app-expo"
  }
}
